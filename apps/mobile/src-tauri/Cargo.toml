[package]
name = "odin-one-mobile"
version = "0.1.0"
description = "Odin One Android shell"
authors = ["Odin One"]
license = ""
repository = ""
edition = "2021"

[lib]
name = "odin_one_mobile_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0.2", features = [] }

[dependencies]
tauri = { version = "2.8.5", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
