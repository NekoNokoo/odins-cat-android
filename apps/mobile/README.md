# Mobile foundation

This folder is reserved for the future mobile shell.

Planned direction:

- shared contracts with desktop
- shared Go core or RPC-compatible backend layer
- iOS shell with native VPN extension bridge
- Android shell with native VPN service bridge

The first release target remains macOS desktop.

